

name: CI Simulation
on:
  push:
    branches:
      - feature-*
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run test script
        run: echo "CI fixed successfully"

