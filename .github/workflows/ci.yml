

name: CI Simulation
on:
  push:
    branches:
      - feature-day4-ci
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run test script
        run: echo "Simulating CI test for Day 4"

